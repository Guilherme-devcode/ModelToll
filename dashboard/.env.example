# Set to "true" to use mock data (no backend required)
VITE_MOCK=true

# API base URL (leave empty to use Vite proxy → http://localhost:8080)
VITE_API_BASE=

# Default API key when localStorage is empty (must match ADMIN_API_KEY in backend)
VITE_DEFAULT_API_KEY=change-me-admin-key
